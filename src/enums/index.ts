export enum StatusCode {
    Succeeded = 200,
    NoContent= 204,
    BadRequest = 400,
    NotFound = 404,
    Conflict = 409,
    Unauthorized = 401,
    Forbidden = 403
}

export enum TypeActionRelease{
    Entrada = 0,
    Saída = 1
}